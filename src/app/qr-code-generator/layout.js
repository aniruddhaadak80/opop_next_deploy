const QrCodePageLayout = ({children}) => {
  return (
    <div>
      <h1 className="text-lg font-mono text-pink-300">Layout for QR page</h1>
       {children}
    </div>
  );
};

export default QrCodePageLayout;